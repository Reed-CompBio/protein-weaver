import React from "react"

export default function QueryError(){
    return(
        <div className="query-error">
            <h3>There was an error in retrieving the network. Check if the Protein ID and/or GO Term are correct</h3>
        </div>
    )
}